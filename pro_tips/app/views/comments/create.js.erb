var comment = {
  'time' : '<%= @comment.created_at %>',
  'commenter' : '<%= User.find(@comment.user_id).name %>',
  'body' : '<%= @comment.body %>',
  'commenter_avatar' : '<%= User.find(@comment.user_id).avatar_url %>',
  'link' : '<%= user_path(@comment.user) %>'
};

Comments.addComment(comment);
